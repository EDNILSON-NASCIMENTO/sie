spring.application.name=sie-backend
server.port=3000
server.servlet.context-path=/api
server.address=0.0.0.0

# Credenciais Twilio em backend/.env (copie de .env.example)
spring.config.import=optional:file:.env[.properties]
